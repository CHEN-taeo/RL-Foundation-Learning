# 扩展定律（Scaling Laws）完整讲解
> 从第一性原理出发，通俗易懂，适合有工科背景的学习者

---

## 目录

1. [在讲扩展定律之前，先弄清楚什么是"规律"](#第一章)
2. [神经网络是什么东西？它在干什么？](#第二章)
3. [扩展定律是怎么被发现的？Kaplan 2020](#第三章)
4. [为什么是幂律？第一性原理解释](#第四章)
5. [三个变量之间的关系：C ≈ 6ND](#第五章)
6. [Kaplan 错在哪里？Chinchilla 定律](#第六章)
7. [扩展定律的边界：哪里会失效？](#第七章)
8. [强化学习和 MARL 里的扩展定律](#第八章)
9. [推理时扩展（Test-Time Scaling）：新的前沿](#第九章)
10. [扩展定律的哲学意义](#第十章)
11. [总结：把所有东西穿成一条线](#总结)

---

<a name="第一章"></a>
## 第一章：在讲扩展定律之前，先弄清楚什么是"规律"

### 1.1 规律的本质

"规律"在科学里，说白了就是一句话：

**你改变一个东西，另一个东西会以可预测的方式跟着变。**

物理里最简单的规律，你一定熟悉：胡克定律，弹簧被拉伸的长度跟拉力成正比，`F = kx`。
改变力 F，位移 x 就跟着等比例变，这是**线性规律**。

但自然界里，更常见、更有意思的一类规律叫做**幂律（Power Law）**。

### 1.2 幂律是什么？

幂律的数学形式很简单：

```
y = a · x^α
```

α 是某个固定的指数，可以是正数、负数，可以是整数也可以是小数。

听起来很平凡？其实不平凡。让我举几个你肯定熟悉的例子：

**例子1：城市人口**

你把全球城市按人口从大到小排一排，编上号。你会发现：
- 第1大城市的人口，大约是第2大城市的1.7倍
- 第1大城市，大约是第4大城市的2.6倍
- 排名越靠后的城市，人口越稀疏，但不是突然断崖式减少

这个规律在任何国家都成立，叫做**齐普夫定律（Zipf's Law）**，它是幂律的一种。

**例子2：地震强度**

你以为7.0级地震只是比6.0级"稍微强一点"？不对。里氏震级每差1级，能量相差约32倍。这就是幂律在作用：能量 E ∝ 10^{1.5M}，也就是震级 M 每增加1，能量增加约32倍。

而且7.0级地震的频率，大约是8.0级的10倍，也是幂律。

**例子3：词频**

英语里，"the"这个词的使用频率，大约是"of"的2倍，是排名第100的词的100倍，是排名第10000的词的10000倍。
词频和排名之间，就是幂律关系。

**例子4：财富分配**

Top 1%的人掌握约40%的财富。不管在哪个国家、哪个年代，财富分布总是幂律的。

### 1.3 幂律最重要的特性：每次翻倍，收益固定百分比

现在我们到了最关键的地方。幂律有一个独特的特性，叫做**尺度不变性（Scale Invariance）**。

这个词听起来很高大上，但说白了就是：

**不管你的起点在哪，把输入翻倍，输出提升的百分比是固定的。**

举个具体例子，假设扩展定律的公式是：
```
L(N) = C · N^{-0.076}
```

（这里 L 是损失，N 是参数量，-0.076 是指数）

那么：
- N 从 100M（1亿）增加到 200M（2亿），翻倍，L 变成原来的 2^{-0.076} ≈ 0.949，**下降约5.1%**
- N 从 1B（10亿）增加到 2B（20亿），再翻倍，L 又下降**约5.1%**
- N 从 10B（100亿）增加到 20B（200亿），继续翻倍，L 还是下降**约5.1%**

关键点在这里：

> 不管你已经做到了多大规模，**每次把规模翻倍，你能得到的提升百分比都是固定的**。

这有两个含义，一正一负：

**正面含义**：你可以一直提升下去，没有天花板（理论上）。

**负面含义**：从绝对量来看，你需要投入越来越多，才能换来同样比例的提升。从100M到200M，投入翻倍换5%。从100B到200B，投入翻倍换的还是5%——但100B是前一个起点的1000倍！

这就是幂律的"残酷"之处：**收益递减，但永不为零，可以无限逼近极限（但永远到不了）**。

这个特性你现在只需要记住一件事：**幂律 = 每次翻倍投入，换来固定百分比的提升**。后面所有的扩展定律，都建立在这个直觉上。

---

<a name="第二章"></a>
## 第二章：神经网络是什么东西？它在干什么？

### 2.1 神经网络是一台函数近似机器

你学过线性代数里的"最小二乘法"：给你一堆散点，你用一条直线 y = ax + b 来拟合它们。

线性拟合有两个参数：斜率 a 和截距 b。你通过"调整 a 和 b 的值"，让直线尽量穿过所有的点。

神经网络做的是完全一样的事情：

- 你有大量的数据（比如，互联网上的所有文字）
- 你想找到一个函数，这个函数能从"前面的词"预测"下一个词"是什么
- 这个函数不是一条直线，而是一个极其复杂的非线性函数
- 这个函数有海量的参数：不是2个，而是几十亿、几百亿个

**参数越多，这个函数的"形状"能变得越复杂，理论上能拟合的数据规律也越精细。**

机械工程的类比：你有一个零件需要加工成复杂的曲面形状。

- 如果你只有3个控制点，你只能做出很简单的曲面
- 如果你有100个控制点，你能做出更精细的形状
- 如果你有10000个控制点，你几乎能加工出任何曲面

神经网络的参数，就是那些控制点。参数量就是控制点的个数。

### 2.2 训练是什么？

训练就是**调参数**——自动地、系统地调。

具体来说：
1. 把大量文字（比如，一段英语文章）喂给模型
2. 让模型猜：这段文字的下一个词是什么？
3. 比较模型猜的和正确答案，算一个"猜错了多少"的分数——这就是**损失函数（Loss）**

> **损失函数具体是什么、怎么算**：想象教一个刚学中文的外国朋友做完形填空——"今天天气很___"，正确答案是"好"。他猜"热"，不算离谱但不对；猜"苹果"，完全离谱。损失函数干的就是这种"打分"的活，只不过语言模型不是在做选择题，它每次要给词表里几万个候选词都打一个概率（"好"35%、"热"20%、"苹果"0.0001%……），损失函数拿这整套概率分布跟"正确答案是好"做比较：**模型给正确答案打的概率越高，损失越小；概率越低，损失越大。** 具体算法叫交叉熵，核心公式是 `损失 = -log(模型给正确答案打的概率)`——这个公式有个直觉性质：概率接近100%时损失趋近于0，概率接近0%时损失趋近于无穷大，而且是指数级加重惩罚。这个设计逼着模型对离谱的错误做出更大幅度的调整，不能敷衍了事。训练的目标，就是让这个"扣分"随着参数不断调整而越来越少。

4. 根据这个分数，把参数稍微调一调，让下次猜得更准
5. 重复这个过程，数十亿、数万亿次

"参数调整"的方法叫做**梯度下降（Gradient Descent）**：从损失函数的"山顶"往下坡走，找最低点。

### 2.3 损失函数的物理含义

这里稍微深入一点，因为理解了损失函数，你才能理解为什么扩展定律的"Y轴"是那个东西。

语言模型用的损失函数叫做**交叉熵损失（Cross-Entropy Loss）**，单位是 bits/token 或 nats/token（两者只是对数底数不同，本质一样）。

它的物理含义：**模型每次猜一个词，平均需要用多少比特来"惊讶"。**

更通俗地说：

- 如果模型完全掌握了这门语言，每次都能猜到下一个词，"惊讶度"为0
- 如果模型完全不会这门语言，每次随机乱猜，设词汇表有 V 个词，那"惊讶度"就是 log₂(V) bits（约 14~16 bits）
- 真实模型的损失介于两者之间，好的模型大约在 **1.5~2.5 bits/token** 左右

> **这两个数字的来源**：上界 14~16 bits 不是任意取的，它对应主流大模型分词器（BPE）的实际词表规模——GPT-2 的词表是 50257 个 token，log₂(50257) ≈ 15.6 bits，均匀乱猜时的"惊讶度"正好是这个值（均匀分布是熵最大的分布）。下界 1.5~2.5 bits/token 来自 Chinchilla 论文（Hoffmann et al. 2022）拟合出的"不可约损失"——他们发现损失曲线不是纯幂律，还要加一个常数项 E（`L(N,D) = E + A/N^α + B/D^β`），代表不管模型和数据怎么堆都降不下去的熵下限。论文估计 E ≈ 1.69 nats/token，换算成 bits（除以 ln2 ≈ 0.693）约等于 2.44 bits/token，这就是下界的出处——语言本身有内禀的不确定性，不是模型的锅。

**扩展定律研究的，就是这个 L（损失）随着 N（参数量）、D（数据量）、C（计算量）变化时的规律。**

---

<a name="第三章"></a>
## 第三章：扩展定律是怎么被发现的？Kaplan 2020

### 3.1 背景：2020年以前，大家怎么看待"更大的模型"？

2020年以前，AI领域对"训练更大的模型"这件事，其实是有顾虑的：

- 更大的模型确实更聪明，但聪明多少？是线性提升还是边际递减到几乎没用？
- 更大的模型需要更多的计算量，这是不是在做无用功？
- 能不能预测"我把模型做大10倍，能提升多少"？

没有人能回答这些问题，因为没有人系统地测量过。

### 3.2 Kaplan et al. 2020：第一次系统测量

2020年1月，OpenAI的Jared Kaplan等人发表了论文《Scaling Laws for Neural Language Models》。

他们做的事情看起来很简单：

**训练了几十个、大小不一的语言模型，从几万个参数到几十亿个参数，系统地记录每个模型的最终损失。**

不是随意地训练几个对比，而是**系统性地扫描**了好几个数量级的参数量范围。

结果他们发现了三条非常稳定的幂律关系：

### 3.3 三条幂律

**第一条：损失随参数量（N）的幂律**

固定数据量和训练时间不变，只增加参数量：

```
L(N) ≈ (N_c / N)^{α_N}
```

实验测出：α_N ≈ **0.076**（指数非常小，说明参数量每翻倍，损失只下降约5%）

**第二条：损失随数据量（D）的幂律**

固定参数量不变，只增加训练数据：

```
L(D) ≈ (D_c / D)^{α_D}
```

实验测出：α_D ≈ **0.095**（略大于α_N，说明数据量的边际价值稍高一些）

**第三条：损失随计算量（C）的幂律**

计算量C（单位：FLOPs，浮点运算次数）表示整个训练过程花了多少"计算"：

```
L(C) ≈ (C_c / C)^{α_C}
```

实验测出：α_C ≈ **0.050**（比N和D都小，因为计算量同时受N和D约束）

### 3.4 这三条幂律说明了什么？

说明了一件划时代的事情：

**语言模型的性能可以被可靠地预测，不需要真的把大模型训出来。**

用小模型做实验，测出幂律指数，然后外推大模型的损失。这就是"用1000美元的实验，预测1亿美元的结果"。

举个具体的例子：

你现在有一个 1亿参数（100M）的模型，训练完之后损失是 2.5。

你想预测：如果我把参数量增加到100亿（10B，增加100倍），损失会是多少？

用幂律公式：
```
L(10B) = L(100M) × (100M/10B)^{-0.076}
       = 2.5 × (1/100)^{0.076}
       = 2.5 × 10^{-0.152}
       = 2.5 × 0.704
       ≈ 1.76
```

参数量增加100倍，损失从2.5降到1.76，**下降约29.6%**。

这听起来不多，但在NLP里，损失下降0.74是巨大的进步，意味着模型在几乎所有任务上都会有显著提升。

### 3.5 Kaplan 定律的实践含义

对于企业来说，这个发现的意义无法估量。

在2020年之前，如果有人说"我要花5亿美元训练一个超大的模型"，没有人能告诉他这个模型到底会有多强。

有了扩展定律之后：

1. 先用小预算（比如100万美元）训练几十个大小不一的小模型
2. 测出幂律指数
3. 用公式预测：花5亿美元训出来的模型，损失会是多少
4. 评估这个损失水平是否值得投入

这直接催生了GPT-3、以及随后的整个大模型军备竞赛。

---

<a name="第四章"></a>
## 第四章：为什么是幂律？第一性原理解释

这是这篇文章最难，也最有意思的部分。

扩展定律是个经验发现，Kaplan他们只是"发现"了它，并没有从理论上"证明"它。那么，**为什么神经网络的性能恰好遵循幂律？**

有好几个角度可以回答这个问题，我们一个一个来。

### 4.1 第一个角度：语言本身的统计结构是幂律的

还记得第一章说的词频？英语词汇的使用频率服从**齐普夫定律**——排名第k的词，使用频率大约是 1/k^α。

> **齐普夫定律具体是什么**：把一门语言里所有词按使用频率从高到低排名，排名第k的词的频率，大约是排名第1的词的 1/k（更一般的形式是 1/k^α，α接近1）。举例：英语里"the"最常见，"of"排第二，频率大约是"the"的一半；排名第10的词，频率大约是"the"的十分之一；排名第1000的词，频率只有"the"的千分之一。这个规律在几乎所有语言、所有语料库里都精确成立，背后有两种解释——一是"最省力原则"（大脑倾向反复调用短小高频的词）、二是这本身是任何离散符号系统的数学必然（即使随机敲键盘生成"词"也会呈现类似分布）。对扩展定律而言，它的意义在于：语言的"可学习性"天生就是不均匀分布的，少数高频词覆盖了语料的大部分，而海量生僻词构成的长尾却只贡献极少的出现次数——模型要吃下这条长尾，需要的参数和数据量呈指数级增长。

这意味着：

- 最常见的几千个词（the, is, of, and, ...），在语料里反复出现，**模型非常容易学会**，哪怕只有很少的参数，也能把这些词处理好
- 中等常见的词（比如 telescope, arithmetic...），出现频率中等，模型需要更多参数才能处理好
- 生僻词（专有名词、术语、罕见词...），出现极少，需要**海量参数和数据**才能掌握

**语言中"词汇学习难度"的分布，本身就是幂律的。**

当你增加参数量时：
- 最先覆盖到的是最高频、最容易学的词汇规律（边际收益最高）
- 随着参数量增加，覆盖的词汇越来越边缘化（边际收益越来越低）

这种"学习难度的幂律分布 × 参数量的增加"的组合，自然产生了损失随参数量的幂律下降。

### 4.2 第二个角度：知识的层次结构

语言模型需要学习的知识，有明显的层次：

**第一层：字符级规律**（最简单）
- 字母 q 后面一般跟 u
- 中文词有常见组合和罕见组合
- 对应的参数量：几百万就够了

**第二层：词法规律**
- 动词的时态变化：walk → walked
- 名词的复数：cat → cats
- 需要的参数量：数千万

**第三层：句法规律**
- 主语谓语宾语的结构
- 从句的嵌套规则
- 需要的参数量：数亿

**第四层：语义规律**
- 苹果是水果，不是动物
- "他把苹果吃了"和"苹果被他吃了"语义相同
- 需要的参数量：数十亿

**第五层：世界知识**
- 中国的首都是北京
- 水在100°C沸腾
- 需要的参数量：数百亿

**第六层：因果推理**
- "如果下雨，那么地面会湿"
- "如果A大于B，B大于C，那么A大于C"
- 需要的参数量：数千亿

**更高层：抽象推理、类比、创造性思维...**

关键规律是：**每往上一层，所需的参数量大约以指数级增加，而学到的知识量（对损失的贡献）却在以幂律递减**。

这种"学习层次的指数代价 × 知识量的幂律贡献"的组合，产生了我们观察到的幂律扩展规律。

### 4.3 第三个角度：信息论视角——压缩的极限

从信息论的角度看，训练神经网络本质上是在做**有损压缩**。

你有一个100GB的语料库，你想把里面的"规律"压缩进一个2GB的神经网络参数文件里。你压进去的，是那些**跨多个文本、可以复用的统计规律**；你扔掉的，是那些只出现一两次、无法归纳的"噪声"。

越大的模型，能捕捉的规律越精细，"压缩效率"越高（损失越低）。

但有一个信息论的极限：**每个数据集都有其固有的"随机性下限"——即使是最完美的模型也无法预测的部分**。这个下限叫做**熵（Entropy）**。

扩展定律告诉你的是：**从当前状态（参数量有限）到"完美模型"（达到熵下限）之间这段距离，随着参数量、数据量、计算量的增加，以幂律速度递减。**

每次参数量翻倍，剩余的可压缩性减少一个固定百分比。但永远不能压缩到零（因为有不可消除的随机性）。

这就从信息论角度解释了为什么必然是幂律，而不是其他形状的曲线。

### 4.4 第四个角度：梯度流动的物理类比

如果你学过流体力学或者热传导（机械工程背景肯定熟悉），可以用这个类比：

梯度信号（训练信号）在神经网络里流动，就像热量在材料里传导。

- 网络越深（层数越多），梯度需要传播的路径越长，衰减越严重
- 但同时，信息的表示能力也在增加
- 这两个效应的竞争，以及参数量在不同深度之间的分配方式，决定了最终的学习效率

不同规模的网络，这个竞争的均衡点不一样，产生的效率也不同。

当你从第一性原理分析这些效应，会发现它们自然地产生幂律式的收益递减。

### 4.5 第五个角度：通用学习理论

从更抽象的角度看，有一个叫做**PAC学习理论（Probably Approximately Correct）**的数学框架，描述了"需要多少样本才能学会一个概念"。

在这个框架里，如果你要学习的"概念"有一定的复杂度（用VC维来衡量），那么学习误差会随着样本量 n 的增加而以 O(1/n^α) 的速度下降——这正是幂律形式！

神经网络的学习，从某种意义上可以被纳入这个框架：参数量越大，能表示的假设空间越大，但同时也需要更多数据来"锁定"正确的假设。这个关系，数学上自然地产生幂律。

---

<a name="第五章"></a>
## 第五章：三个变量之间的关系：C ≈ 6ND

在讨论如何最优地分配计算预算之前，我们需要先建立一个重要的关系式。

### 5.1 参数量、数据量和计算量的关系

在训练语言模型时，三个核心变量之间有一个近似的线性关系：

```
C ≈ 6 × N × D
```

其中：
- **C**：总计算量，单位是 FLOPs（浮点运算次数）
- **N**：模型参数量（parameter count）
- **D**：训练用的 token 数

### 5.2 为什么是"6"？

这个"6"来自神经网络前向传播和后向传播的计算量分析：

**前向传播（Forward Pass）**：
- 对于矩阵乘法（神经网络的核心运算），每个参数需要参与1次乘法+1次加法
- 所以每个 token 的前向传播计算量 ≈ **2N**（每个参数2次运算）

**后向传播（Backward Pass）**：
- 计算梯度（∂L/∂参数）的计算量，大约是前向传播的2倍
- 所以后向传播计算量 ≈ **4N**

**总计**：2N（前向）+ 4N（后向）= **6N** per token

那么，训练D个 token 的总计算量就是：
```
C = 6N × D = 6ND
```

### 5.3 用这个公式验算 GPT-3

GPT-3 的基本参数：
- N = 175B（1750亿参数）
- D = 300B（3000亿 tokens）

代入公式：
```
C = 6 × 175×10⁹ × 300×10⁹ = 3.15 × 10²³ FLOPs
```

这是一个天文数字。一块 A100 GPU 每秒能做约 3×10¹⁴ FLOPs。

如果用1024块 A100 并行：
```
时间 = 3.15×10²³ / (1024 × 3×10¹⁴) ≈ 10⁶ 秒 ≈ 11.6 天
```

（实际上 GPT-3 用了约 1024 块 A100，训练了大约 34 天——效率损失和通信开销导致实际时间更长，但数量级是对的。）

### 5.4 这个公式有什么用？

**用处1：在固定计算预算下，找最优的 N 和 D 分配**

给定计算量 C，你需要在 N 和 D 之间做取舍。这就引出了下一章的核心问题：Kaplan 和 Chinchilla 给出了不同的答案。

**用处2：估算训练成本**

如果你想训练一个 70B 的模型，训练 1T tokens，计算量大约是：
```
C = 6 × 70×10⁹ × 10¹² = 4.2×10²³ FLOPs
```

按当前 A100 算力价格，大约需要花多少钱可以直接算出来。

**用处3：设计自己的实验**

你在做 MARL 实验，想知道"我能负担得起多大规模的实验"？先算 C，再根据你的GPU资源反推能跑多久。

---

<a name="第六章"></a>
## 第六章：Kaplan 错在哪里？Chinchilla 定律

这是扩展定律历史上最重要的一次"修正"。

### 6.1 Kaplan 的结论：优先扩大模型

Kaplan et al. 在2020年得出了一个结论：

**给定固定的计算预算 C，应该把大部分计算量用来增大模型（N），而不是增加数据量（D）。**

他们的最优分配比例大约是：
- N ∝ C^{0.73}（参数量随计算量的 0.73 次方增长）
- D ∝ C^{0.27}（数据量随计算量的 0.27 次方增长）

这意味着：当你的计算预算翻倍时，你应该把大约73%的增量用来做更大的模型，27%用来喂更多数据。

这个结论直接指导了 GPT-3 的设计：1750亿参数，但只训练了约 300B tokens。

### 6.2 Chinchilla 定律：2022年的颠覆

2022年，DeepMind 的 Hoffmann 等人发表了《Training Compute-Optimal Large Language Models》，提出了挑战 Kaplan 结论的"Chinchilla 定律"。

他们的实验做得更细致：**不只是固定步数、改变模型大小，而是固定总计算量、系统地扫描更多的 N/D 组合。**

结论震惊了整个领域：

**Kaplan 的最优分配是错的。正确的最优分配是：N 和 D 应该等比例增长。**

Chinchilla 定律：
- N ∝ C^{0.5}（参数量随计算量的平方根增长）
- D ∝ C^{0.5}（数据量也随计算量的平方根增长）

更具体地说，最优比例是：

> **每个参数需要约 20 个 token 的数据来训练。**
>
> 即：D_optimal ≈ 20 × N

这就是著名的"Chinchilla 比例"或"20倍规则"。

### 6.3 这意味着 GPT-3 严重训练不足

GPT-3：N = 175B 参数

按 Chinchilla 最优比例，应该用的数据量：
```
D_optimal = 20 × 175B = 3.5万亿 tokens
```

但 GPT-3 实际只用了 300B tokens，只有最优数据量的 **8.6%**。

换句话说，**GPT-3 的参数规模远超过当时的数据量所能"支撑"的程度。大量参数其实是浪费的，没有被充分训练。**

### 6.4 Chinchilla 验证：用 70B 超越 280B

为了验证这个理论，DeepMind 训练了一个叫做 Chinchilla 的模型：
- N = 70B（参数量是 Gopher 的 1/4）
- D = 1.4万亿 tokens（比 Gopher 多了约 4 倍）

计算量和 Gopher（280B 参数，300B tokens）大致相当。

结果：**Chinchilla 在所有基准测试上全面超越了 Gopher。**

用 1/4 的参数，达到了更好的性能——只需要遵循正确的 N/D 比例。

### 6.5 Kaplan 为什么会犯这个错误？

原因很有意思，值得深究。

Kaplan 团队在做实验时，为了方便比较，固定了"训练步数（steps）"来控制计算量，而不是固定"总 token 数"。

这导致了一个系统性偏差：

- 大模型每步学到的东西更多（参数量大，表达能力强），在固定步数下表现更优
- 小模型每步学得少，但如果给它更多步数（更多数据），性能会继续提升

Kaplan 只看到了"固定步数下大模型优于小模型"，就错误地外推成"应该优先扩大模型"。

Chinchilla 修正了这个偏差：用"固定总计算量"来比较，小而充分训练的模型往往比大而训练不足的模型更好。

### 6.6 Chinchilla 对行业的影响

Chinchilla 定律发表后，整个行业都重新调整了方向：

- **LLaMA（Meta，2023）**：7B/13B/30B/65B，按 Chinchilla 比例，使用 1~1.4T tokens 训练
- **Mistral（2023）**：7B 参数，大量数据，远超 Kaplan 时代的同规模模型
- **Falcon（TII，2023）**：类似路线

这些"小而精"的开源模型，能够用远比 GPT-3 小的参数量，达到相近甚至超越的性能，Chinchilla 定律是关键原因之一。

---

<a name="第七章"></a>
## 第七章：扩展定律的边界：哪里会失效？

扩展定律不是万能灵药。它有边界，有失效的条件。

### 7.1 边界一：数据墙（Data Wall）

Chinchilla 告诉你，每个参数需要 20 个 token 的数据。

问题来了：互联网上的高质量文本是有限的。

有研究估计，全球高质量英语文本大约有 4~8 万亿 tokens。

如果你想训练一个 4000 亿参数的模型（Chinchilla 最优），你需要：
```
D_optimal = 20 × 4000亿 = 8万亿 tokens
```

这已经接近了互联网高质量文本的天花板。

那接下来怎么办？主要有几个方向：

**方向1：多语言数据**——把中文、法语、德语、日语、阿拉伯语等都用上，数据量直接扩大几倍

**方向2：合成数据**——让 AI 生成数据来训练 AI（这是有争议的方向，质量难以保证）

**方向3：多模态数据**——图片、视频、音频、代码……这些数据量远超纯文本

但无论如何，随着数据接近天花板，扩展定律的斜率会变平，投入产出比会下降。

### 7.2 边界二：涌现能力（Emergent Abilities）

这是扩展定律最令人着迷、也最令人困惑的地方。

按照扩展定律，模型性能应该**平滑、可预测地**随规模增长——幂律曲线是光滑的，没有突变。

但实际上，在某些特定任务上，出现了"相变"式的涌现：

**现象**：某些能力在小模型里几乎为零，越过某个规模阈值后突然大幅提升，就像水从液态突然变成气态一样。

**例子**：
- **多步算术**（如 5 位数乘法）：在约 50B 参数以下几乎不会，超过后突然能做
- **常识推理**：类似的涌现模式
- **代码生成**：特别是需要理解复杂逻辑的代码
- **In-context Learning**（情境学习）：在提示词里给几个例子，就能学会新任务

这看起来和"平滑的幂律"是矛盾的。为什么？

**一个可能的解释**：我们用来衡量这些任务的指标是不连续的（"答对/答错"只有0/1），所以即使模型的"内在能力"在平滑地增长，在这些离散指标上看起来也像突然涌现。

如果你换成更连续的指标（比如"每个token的概率"），涌现可能就消失了，变回平滑的幂律曲线。

但这个争论还未定论。部分研究认为涌现是真实的，不只是指标的伪影。

**对你的实践意义**：不要以为小模型完全无用。某些能力，确实需要规模门槛，低于这个门槛，怎么调都没用；超过之后，才能开始发挥。

### 7.3 边界三：不同任务的扩展速度不同

扩展定律最初是在"下一个词预测"这个任务上发现的。

不同任务，扩展定律的斜率（指数α）是不一样的：

- **语言建模**（next-token prediction）：斜率稳定，幂律成立
- **翻译任务**：斜率类似，但有轻微不同
- **数学推理**：扩展定律成立，但达到人类水平需要的规模更大
- **代码生成**：类似数学推理
- **博弈类游戏**（围棋、象棋）：扩展定律成立，但在不同维度（模型大小 vs 搜索量 vs 自对弈数据量）的权重不同

实践上：**你不能把"语言模型的扩展定律斜率"直接套用到其他任务上。** 每个任务域可能有自己的幂律系数。

### 7.4 边界四：架构依赖

Kaplan 和 Chinchilla 的实验，都是在 **Transformer 架构**上做的。

换成其他架构（比如近年热门的 Mamba、RWKV 等），扩展定律是否仍然成立？成立的话指数是多少？

目前来看：
- Transformer 仍然是扩展最好的架构
- 新架构（如 Mamba）在小规模下有优势，但大规模时往往逊于 Transformer
- 没有人能保证 Transformer 是"最终答案"

### 7.5 边界五：数据质量的影响

扩展定律假设的是"高质量数据"。

如果你用低质量数据（网络爬虫、重复内容、噪声文本）扩大数据量，幂律的斜率会显著减小。

换句话说：**200B 高质量 tokens 往往比 2T 低质量 tokens 更有用。**

这就是为什么现在很多工作聚焦在"数据清洗和筛选"上——LIMA、Phi 系列等模型，用极少但高质量的数据，训出了远超预期的模型性能。

---

<a name="第八章"></a>
## 第八章：强化学习和 MARL 里的扩展定律

这是和你最相关的部分，所以我多花一些篇幅。

### 8.1 RL 和有监督学习的本质区别

有监督学习（语言模型训练）的扩展定律好研究，因为：
- **数据是固定的**：互联网文本是外部给定的
- **监督信号是密集的**：每个 token 都有标签（就是下一个 token 本身）
- **分布是静态的**：训练数据的分布不随训练过程变化

强化学习完全不同：

1. **数据是自己生成的**：智能体通过和环境互动来收集数据，数据质量依赖当前策略，策略依赖训练——这是个反馈循环
2. **奖励是稀疏的**：有时候需要做几百步才能得到一次反馈，梯度信号极其稀疏
3. **分布是动态的（非平稳性）**：随着策略改进，遇到的状态分布在不断变化——用旧数据训出来的模型，在新状态下可能表现很差
4. **探索是核心问题**：增大模型不直接帮助探索，甚至可能让探索更保守（大模型更容易过拟合到已见过的状态）

这些差异导致：**RL 中的扩展行为远比语言模型复杂，且更不稳定。**

### 8.2 早期 RL 扩展的教训：DQN on Atari

OpenAI 在研究 Atari 游戏时发现了一个反直觉的现象：

给 DQN（Deep Q-Network）增加参数量，**不一定有用，有时候还会变差。**

原因是：
- 大模型更容易记住 replay buffer 里的"旧经验"，对新情况适应慢
- 在非平稳的 RL 环境里，大模型更容易出现"灾难性遗忘"
- 网络越大，Q值的估计越不稳定，训练越难收敛

这告诉我们：**在 RL 里，"更大"不是免费的午餐，需要配合相应的训练技巧才有用。**

### 8.3 AlphaZero 系列：有效的 RL 扩展

AlphaGo、AlphaZero 系列是目前最成功的大规模 RL 扩展案例。

在这个系列里，扩展体现在三个维度上：

**维度1：网络大小（N）**——更大的残差网络，能表示更复杂的棋局特征

**维度2：自对弈数据（D）**——更多的自对弈游戏，产生更多训练样本

**维度3：搜索量（MCTS 模拟次数）**——每次走棋时，搜索更多可能的后续路径

AlphaZero 的关键发现：
- 三个维度**缺一不可**，只扩一个效果有限
- 扩展带来的收益是**亚线性的**（幂律形式，α < 1），但确实稳定存在
- 与 Transformer 语言模型相比，RL 系统的扩展斜率更平，收益更慢

### 8.4 RLHF 中的扩展：GPT-4 的路线

最近这几年，最成功的 RL 应用是 **RLHF（人类反馈强化学习）**——这是 ChatGPT、Claude 等系统背后的核心技术。

RLHF 的扩展规律和纯 RL 不一样，因为它建立在预训练语言模型的基础上：

**发现1：基础模型越大，RLHF 效果越好**

一个 7B 的基础模型，经过 RLHF 能变成很好的对话助手。
一个 70B 的基础模型，经过相同的 RLHF 过程，能变成更好的对话助手。

这说明：**RLHF 在"引导"模型，而不是"教会"模型新知识。** 知识是在预训练阶段学的，RLHF 只是对齐方向。基础能力越强，引导的效果越好。

**发现2：Reward Model 的参数量也需要扩展**

在 RLHF 里，有一个"奖励模型（Reward Model）"负责给策略模型的输出打分。

如果奖励模型太小，它的评分质量成为瓶颈，即使策略模型做得再好，奖励模型也分辨不出来——出现"奖励欺骗（Reward Hacking）"问题。

一般规律：**奖励模型的参数量应该不低于策略模型的参数量**，才不会成为扩展的瓶颈。

### 8.5 MARL（多智能体 RL）中的扩展：你最应该关注的

多智能体强化学习（MARL）的扩展，是目前最前沿、最不确定的领域。

**已知发现一：CTDE 框架里，Critic 的扩展是有效的**

在中心化训练去中心化执行（CTDE，Centralized Training Decentralized Execution）框架下：
- 中心化 Critic 可以按照单智能体扩展定律的方式增大参数
- 观察到了类似的幂律收益

但整体效果比单智能体弱：因为 Critic 需要处理所有智能体的状态，输入维度随智能体数量线性增长，这大大增加了学习难度。

**已知发现二：智能体数量的扩展不是幂律，而是组合爆炸**

一个 10 个智能体的协作任务，不是"一个智能体任务的10倍难度"。

因为联合状态空间的大小，随智能体数量呈**指数级增长**：
- 1个智能体：状态空间 |S|
- 2个智能体：状态空间 |S|²
- N个智能体：状态空间 |S|^N

这是**组合爆炸问题**，扩展定律在这里完全失效。需要用特殊的方法来缓解：参数共享、注意力机制、图神经网络等。

**已知发现三：参数共享是 MARL 的"数据复用"神器**

在同类智能体之间共享策略网络参数，是 MARL 里最有效的扩展技巧之一。

原理：10个智能体，每个智能体每一步都给共享网络提供一条梯度。相当于数据量增加了10倍，但参数量没有增加。

这类似于语言模型里的"更多数据"——按 Chinchilla 定律，数据量增加能有效降低损失。

在 MARL 里，参数共享就是"用N倍数量的数据来训练同一个网络"，效果非常显著。

**MARL 扩展的核心挑战（你需要重点关注的）**

对于你的方向——**养老机器人 MARL**——最相关的挑战是：

1. **真实世界数据稀缺**：机器人操控数据比互联网文本少太多，数据墙来得更早
2. **任务异质性**：养老场景里的任务多样且复杂（搬运、对话、监控、应急），不同任务的扩展规律可能完全不同
3. **安全约束**：在真人身边工作的机器人，不能靠"大量失败经验"来学习，探索成本极高

目前最前沿的方向（具身智能 MARL）：
- Google RT-2、OpenVLA 等工作：把语言模型直接用于机器人控制，利用语言模型已有的扩展定律收益
- 发现：语言模型参数量增大，对机器人操控能力也有提升，但比纯语言任务效果弱（因为缺乏机器人操控数据）

---

<a name="第九章"></a>
## 第九章：推理时扩展（Test-Time Scaling）——新的前沿

### 9.1 一个新的扩展维度

扩展定律的经典版本关注的是：**训练时**如何分配更多计算量（N、D、C）。

但2024年出现了一个新的方向：**推理时（Inference Time）也可以扩展。**

具体来说：给模型更多"思考时间"，让它在输出答案之前生成大量的中间步骤（Chain of Thought，思维链），可以显著提升性能——特别是在数学、逻辑推理、代码等需要多步思考的任务上。

这就是 **GPT-o1、Claude 3.7 Sonnet、DeepSeek-R1** 等模型背后的核心技术。

### 9.2 推理时扩展也遵循幂律

OpenAI 和 DeepMind 的研究发现：

**在推理时投入更多计算量（更多思考 token），模型在数学和推理任务上的性能提升，也遵循幂律。**

具体来说：

- 让模型生成更长的思维链（更多 token），每次token数翻倍，正确率以固定百分比提升
- 给模型机会做多次尝试（Best-of-N sampling），N 翻倍，最好答案的质量以幂律提升
- 用搜索（比如 MCTS）引导推理过程，搜索量翻倍，解题成功率以幂律提升

### 9.3 推理时扩展 vs 训练时扩展

| | 训练时扩展 | 推理时扩展 |
|---|---|---|
| **投入** | 训练 FLOPs（一次性）| 推理 FLOPs（每次请求）|
| **产出** | 模型权重永久变好 | 仅当次问题的回答更好 |
| **成本结构** | 高一次性成本，低边际成本 | 低一次性成本，高边际成本 |
| **适合场景** | 通用能力提升 | 高难度、高价值问题 |
| **幂律成立？** | 成立（Kaplan/Chinchilla）| 成立（新发现）|

对于你做 MARL 研究最重要的启发是：

**在执行一个复杂的多智能体任务之前，让系统"多想一想"（用更多推理时计算量），可能比单纯增大模型参数量更高效，成本也更低。**

比如：养老机器人在执行一个复杂的护理任务之前，先用模型进行"规划阶段"（生成完整的行动序列，预测各种意外情况），再执行，效果可能远好于直接决策。

### 9.4 推理时扩展的边界

推理时扩展也不是万能的：

- **对于简单问题，效果有限**：一个 5+3 的加法题，给再多思考时间也不会更准
- **对于需要外部知识的问题，效果有限**：推理时无法获取训练数据之外的知识
- **成本高**：推理时 token 很贵，相比一次性训练成本，重复推理成本可能更高

所以推理时扩展和训练时扩展是互补的，不是替代关系。

---

<a name="第十章"></a>
## 第十章：扩展定律的哲学意义

到了最后，我想聊一些更底层的东西。不只是技术，而是哲学。

### 10.1 扩展定律告诉我们什么？

扩展定律的存在，在某种程度上是令人震惊的。

它意味着：**语言层面的"智能"，可以通过暴力叠加计算量来逼近。不需要精心设计的规则，不需要符号系统，不需要知识图谱，只要足够多的参数、足够多的数据、足够多的计算，语言能力就会自然涌现。**

这在2015年之前是大多数人不相信的。当时主流观点是：语言理解需要"真正的理解"，需要逻辑推理、知识表示、手工规则。

扩展定律打了所有这些人的脸。

### 10.2 但扩展定律没有告诉我们什么？

损失从2.5降到1.5，模型变"聪明"了吗？还是只是"更会猜词"了？

涌现能力的出现，让这个问题更难回答。一个突然能做五位数乘法的模型，是真的"理解了数学"，还是"找到了一个足够好的近似模式"？

目前没有人知道答案。但有一点是确定的：

**扩展定律给了我们一个工具，让我们能在不理解"智能是什么"的情况下，仍然可以预测和构建越来越"有能力"的系统。**

这本身就是一种深刻的工程智慧：你不需要理解万有引力的本质，就能用牛顿力学发射卫星。同理，你不需要理解智能的本质，就能用扩展定律构建有用的 AI 系统。

### 10.3 扩展定律和你的学习路径

扩展定律还有一个不太显而易见的应用：

**你自己的学习，也有"扩展定律"。**

类比：
- 你的"参数量" = 你掌握的知识深度和广度
- 你的"数据量" = 你做过的项目、写过的代码、跑过的实验
- 你的"计算量" = 你花的时间和认知精力

你的学习也遵循幂律：
- 最基础的概念（Q-Learning、Bellman 方程）给你最大的初始收益
- 中等难度的概念（QMIX、MAPPO、CTDE）需要更多时间，但每个知识点的边际收益在下降
- 最前沿的概念（具身智能、世界模型、MARL 扩展）需要大量基础积累，收益要到很久之后才显现

**慢就是快，打好地基——这就是你个人学习的 Chinchilla 定律。**

每个参数（知识点）需要约20个 token（实践机会）来"训练"。

光看书看论文，没有动手写代码，你的知识是"训练不足"的——就像 GPT-3 那样，参数量挺大，但真正的能力没有被激活。

### 10.4 扩展定律会永远成立吗？

扩展定律是一个**经验规律**，不是一个**自然定律**。

它在特定条件下（Transformer 架构、语言建模任务、特定数据分布）已经稳定成立了好几个数量级。但它不会永远成立——没有任何经验规律会永远成立。

重要的是理解它**为什么**成立，这样当它开始失效的时候，你能看出来，能找到下一个"定律"。

**乐观的一面**：推理时扩展、合成数据、多模态扩展，都是新的维度，还有很长的路可以走。

**悲观的一面**：数据墙真实存在，能耗问题日益严峻，斜率可能在某些维度上已经开始放缓。

不管如何，**理解扩展定律，是理解当代 AI 进展的基石**。

---

<a name="总结"></a>
## 总结：把所有东西穿成一条线

好，用最简洁的语言把今天学到的所有东西串起来：

### 核心知识清单

| # | 知识点 | 一句话总结 |
|---|---|---|
| 1 | **幂律** | 投入翻倍，输出提升固定百分比；收益递减但不为零 |
| 2 | **神经网络** | 函数近似机器；参数越多，能表达的函数越复杂 |
| 3 | **损失函数** | 衡量"猜错了多少"（bits/token）；越低越好 |
| 4 | **Kaplan 定律** | L 和 N、D、C 之间都是幂律关系（2020） |
| 5 | **为什么是幂律** | 语言统计结构是幂律的；知识层次需要指数代价；信息论的压缩极限 |
| 6 | **C ≈ 6ND** | 计算量 = 6 × 参数量 × Token数；连接三个核心变量 |
| 7 | **Chinchilla 定律** | 最优分配：N ∝ C^0.5，D ∝ C^0.5；每个参数需 20 个 token |
| 8 | **GPT-3 的错误** | 1750亿参数，只用300B tokens，严重训练不足 |
| 9 | **扩展边界** | 数据墙、涌现、架构依赖、数据质量 |
| 10 | **RL 中的扩展** | 比语言建模复杂；RLHF 框架里类似定律成立 |
| 11 | **MARL 中的扩展** | 组合爆炸是核心问题；参数共享是最有效工具 |
| 12 | **推理时扩展** | 更多"思考时间"也遵循幂律提升；与训练时扩展互补 |
| 13 | **哲学意义** | 暴力计算可逼近某种智能，但不告诉我们智能是什么 |

### 一句话概括

**给定足够的参数、足够的数据、足够的计算，神经网络的性能会以可预测的幂律方式提升——这个规律稳定到让人可以据此决定投入数十亿美元的资本，也是你进入 MARL/具身智能领域必须理解的底层逻辑。**

---

## 附录：扩展定律关键公式速查表

### 核心幂律公式（Kaplan 2020）

```
L(N) ≈ (Nc/N)^0.076       # 损失随参数量
L(D) ≈ (Dc/D)^0.095       # 损失随数据量
L(C) ≈ (Cc/C)^0.050       # 损失随计算量
```

### 联合公式（最优训练，Chinchilla 视角）

```
C ≈ 6ND                    # 计算量≈6×参数量×Token数
N_opt ≈ √(C/120)           # Chinchilla 最优参数量
D_opt ≈ 20 × N_opt         # Chinchilla 最优数据量
```

### 直觉速记

| 如果你... | 性能提升约... |
|---|---|
| 参数量 ×2（翻倍） | 损失降低 ~5% |
| 参数量 ×10（10倍） | 损失降低 ~15-17% |
| 参数量 ×100（100倍） | 损失降低 ~30% |
| 数据量 ×2 | 损失降低 ~6.5% |
| 计算量 ×10 | 损失降低 ~11% |

### 判断一个模型是否"Chinchilla 最优"

```
D / N 的比值应约等于 20。

GPT-3：300B tokens / 175B params ≈ 1.7   → 严重训练不足
LLaMA-3-8B：15T tokens / 8B params ≈ 1875  → 超出 Chinchilla 最优（刻意过训练，用于推理效率）
```

> 注意：过训练（超过 Chinchilla 最优比例）在"部署优先"场景下是合理的——你用更多算力来训练，换取推理时更小的模型，节省长期推理成本。LLaMA 系列就是这个思路。

### 相关论文原文索引

| 论文 | 年份 | 核心贡献 |
|---|---|---|
| Kaplan et al., *Scaling Laws for Neural Language Models* | 2020 | 发现三条幂律 |
| Hoffmann et al., *Training Compute-Optimal LLMs (Chinchilla)* | 2022 | 修正最优分配比例 |
| Anil et al., *Gemini: A Family of Highly Capable Multimodal Models* | 2023 | 多模态扩展定律 |
| Snell et al., *Scaling LLM Test-Time Compute...* | 2024 | 推理时扩展定律 |
| Brown et al., *Language Models are Few-Shot Learners (GPT-3)* | 2020 | 涌现能力首次大规模观察 |
| Wei et al., *Emergent Abilities of Large Language Models* | 2022 | 系统性研究涌现现象 |

---

*写于2026年6月30日。适配陈韬的 MARL+具身智能+养老机器人方向。*
*如有问题或需要深入某个部分，下次来问。*
